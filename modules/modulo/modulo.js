class Modulo {
    constructor(id, idCurso, titulo, ordem) {
        this.ID_Modulo = id;
        this.ID_Curso  = Number(idCurso);
        this.Titulo    = titulo;
        this.Ordem     = Number(ordem);
    }
}
